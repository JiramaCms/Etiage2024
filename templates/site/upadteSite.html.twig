{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row justify-content-center">
            <!-- Début de la carte Bootstrap -->
            <div class="card col-md-8 mt-4"> <!-- Ajout de mt-4 pour le margin-top -->
                <div class="card-body">
                    <h5 class="card-title">Modifier le site</h5>
                    
                    <!-- Début du formulaire -->
                    {{ form_start(form, { attr: { novalidate: 'novalidate', class: 'row g-3' } }) }}
                    
                    <div class="col-12">
                        {{ form_label(form.libelle, "Objectif title", {'label_attr': {'class': 'form-label'}}) }}
                        {{ form_widget(form.libelle, {'attr': {'class': 'form-control'}}) }}
                        <small>{{ form_help(form.libelle) }}</small>
                        <div class="form_error">
                            {{ form_errors(form.libelle) }}
                        </div>
                    </div>

                    <div class="col-12">
                        {{ form_label(form.adresse, "Libelle", {'label_attr': {'class': 'form-label'}}) }}
                        {{ form_widget(form.adresse, {'attr': {'class': 'form-control', 'placeholder': 'mettez le Libelle ici'}}) }}
                        <small>{{ form_help(form.adresse) }}</small>
                        <div class="form_error">
                            {{ form_errors(form.adresse) }}
                        </div>
                    </div>

                    <div class="col-12">
                        {{ form_label(form.coord, "Début du projet", {'label_attr': {'class': 'form-label'}}) }}
                        {{ form_widget(form.coord, {'attr': {'class': 'form-control'}}) }}
                        <small>{{ form_help(form.coord) }}</small>
                        <div class="form_error">
                            {{ form_errors(form.coord) }}
                        </div>
                    </div>

                    <!-- Bouton de soumission -->
                    <div class="text-center">
                        <input type="submit" value="Modifier le site" class="btn btn-secondary">
                    </div>
                    
                    {{ form_end(form) }}
                    <!-- Fin du formulaire -->
                </div>
            </div>
            <!-- Fin de la carte Bootstrap -->
        </div>
    </div>
{% endblock %}
